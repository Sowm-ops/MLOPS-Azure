# Git / CI metadata
.git
.github
.drone.yml
Jenkinsfile
docker-compose.yml

# Python cache / venv
__pycache__
*.py[cod]
*$py.class
.pytest_cache
.coverage
htmlcov
env
venv
.venv

# DVC internals not needed in image
.dvc/cache
.dvc/tmp

# Usually not needed inside the API image (DVC pulls artifacts during CI)
data

# Notebooks/docs
notebooks
docs

# OS junk
.DS_Store
